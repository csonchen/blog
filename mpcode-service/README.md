# 生成小程序码接口服务

## start

```
## install packages
npm install

## run project
// On MacOS or Linux, run the app with this command
DEBUG=mpcode-service:* npm start

// On Windows Command Prompt, use this command
SET DEBUG=mpcode-service:* & npm start
```